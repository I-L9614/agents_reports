services:
  mongodb:
    image: mongo:7
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: itamar
      MONGO_INITDB_ROOT_PASSWORD: B3UNAbgEyabV5
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: