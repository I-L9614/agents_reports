open new terminal to each file and get insid the file

then run "npm i" that would install al the packages you need for the project

then run in each terminal "npm run dev"


press on the link it the client terminal

the admin code is "WO7" and his password is "james bond"